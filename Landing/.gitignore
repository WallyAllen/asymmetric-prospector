# Sistema operativo
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp

# Entornos / secretos (por si se agrega tooling con build en el futuro)
.env
.env.*
node_modules/

# Configuración local de Claude Code (no compartida en el repo)
.claude/settings.local.json

# Documentos internos de estrategia de negocio — no se publican
business_system_report.md

# Artefactos temporales de delegacion a Gemini CLI
.gemini/
